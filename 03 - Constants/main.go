package main

import "fmt"

const (
	isAdmin bool   = false
	name    string = "nima"
	role    string = "developer"
	job            = "full stack"
	nickName
)

func main() {
	const word string = "foo"
	const decoded float64 = 289.2644
	const Mynum int = 30
	const nickName int = 30
	fmt.Println(Mynum, word, decoded)
}
